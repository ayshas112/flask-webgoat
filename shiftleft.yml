build_rules:
  - id: non_highs
    severity: [critical, high]
    finding_types: [vuln]
  - id: only_reachable
    severity: [critical, high]
    finding_types: [oss_vuln]
    options:
      reachable: true
